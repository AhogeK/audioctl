---
description: "执行 C/ObjC 音频驱动的静默构建与全量测试"
---

# 🚀 构建与测试执行流

请严格按顺序在终端执行以下操作，并在遇到任何红色 Error 时立即终止并汇报：

1. 执行免重启的快速安装：
   ```bash
   ./scripts/install.sh install --no-coreaudio-restart
   ```
2. 进入 Debug 目录运行测试门控：
   ```bash
   cd cmake-build-debug && ctest --output-on-failure
   ```
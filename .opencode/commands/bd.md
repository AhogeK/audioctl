---
description: "Beads 任务管理协议：结构化工程任务的生命周期管理"
---

# 🔮 Beads 任务管理协议

## 📋 核心理念

**bd (beads)** 是本项目的结构化任务跟踪系统，与 `memory-bank` 的定位差异如下：

| 系统              | 职责                              | 内容类型                   |
|:----------------|:--------------------------------|:-----------------------|
| **memory-bank** | AI 自身的思考链、架构决策、当前战区上下文          | 非结构化的 Markdown，记录"为什么" |
| **bd (beads)**  | 工程任务的依赖树、优先级、验收标准，支持多会话/多 Agent | 结构化的 JSONL，记录"做什么"     |

## 🚀 快速参考

### 会话启动检查清单

    bd ready                          # 查看无阻塞的可执行任务
    bd list --status in_progress      # 查看当前进行中的任务
    bd show <id>                      # 查看任务详细描述与依赖关系

### 任务生命周期

    # 创建任务
    bd create "修复 DoIOOperation 非交错模式适配" \
      -p 0 \
      -t bug \
      -d "FIX_2026_02_13 中的音频回调竞态条件"

    # 认领任务
    bd update <issue-id> --status in_progress

    # 完成任务
    bd close <issue-id>

> **协作说明**：AI 执行 bd 命令修改任务状态，人类负责将 `.beads/issues.jsonl` 的变更通过 Git 提交推送。

### 依赖管理

    bd dep add <blocker-id> <blocked-id>   # 添加阻塞关系（A 阻塞 B）
    bd dep tree <issue-id>                 # 可视化依赖树
    bd blocked                             # 查看所有被阻塞的任务

## 🎯 与现有工作流的集成

### 唤醒时（boot.md）

- 读取 `memory-bank/progress.md` → AI 的上下文战区
- 执行 `bd ready` → 结构化的待办任务清单
- **人类前置确认**：先完成 `git pull` 确保本地最新

### 休眠时（sleep.md）

- AI 更新 `memory-bank/progress.md` 存档推理链
- AI 执行 `bd close` / `bd create` 同步任务状态
- **人类落地**：决定是否提交、运行测试、执行 `git commit` / `git push`

### 紧急问题升级

当 AI 在 `devlog/ask_expert.txt` 中标记技术疑点时，应同步创建 bd issue：

    bd create "【专家会诊】GetZeroTimeStamp 时钟计算疑点" \
      -p 0 \
      -t investigation \
      --design "参见 devlog/ask_expert.txt"

## ⚠️ 关键约束

- **禁止重复记录**：bd 中的任务描述应简洁，详细技术分析记录在 `memory-bank/decisions.md`
- **优先级映射**：
    - `-p 0` = P1（阻塞性 Bug / 核心功能）
    - `-p 1` = P2（重要但不紧急）
    - `-p 2` = P3（优化项）
- **权限边界**：
    - AI 负责执行 bd 命令管理任务状态
    - 人类负责所有 Git 操作（commit/push/pull）
    - AI 在每个可提交小步完成后进入 Checkpoint，等待人类确认

## 📊 任务类型定义

| Type            | 使用场景                 |
|:----------------|:---------------------|
| `bug`           | 已知功能缺陷（如驱动崩溃、卡顿）     |
| `feature`       | 新功能开发（如新增音量控制 API）   |
| `refactor`      | 代码重构（如 Lock-free 改造） |
| `investigation` | 技术调研（如 HAL 插件替代方案）   |
| `test`          | 测试用例补充               |

## 🔗 参考资源

- [Beads GitHub](https://github.com/steveyegge/beads)
- [AI Agent 最佳实践](https://betterstack.com/community/guides/ai/beads-issue-tracker-ai-agents/)

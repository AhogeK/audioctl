---
description: "初始化工作流：唤醒记忆体并同步代码库最新状态"
---

# 🚀 系统唤醒与状态同步指令

请严格按照以下顺序执行初始化操作，不要有任何遗漏：

1. **环境嗅探**：全局快速扫描当前代码库结构（重点关注 `src/` 和 `include/` 的变动）。
2. **记忆挂载**：静默读取 `memory-bank/progress.md` 与 `memory-bank/implementation-plan.md`。
3. **状态汇报**：结合你扫描到的代码库现状与记忆体中的进度，使用 `- [ ]` 任务列表在对话框中向我汇报当前所处的开发阶段，并列出接下来建议执行的前
   3 个动作。

> **注意**：如果发现 `memory-bank` 目录或相关文件不存在，请根据当前项目状态主动帮我创建并初始化它们。
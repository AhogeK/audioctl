# 🧪 测试协作协议

> **更新**: 2026-02-15

## 🚫 AI 严禁

- `./scripts/install.sh install`（含 `--no-coreaudio-restart`）
- 切换音频设备 / 播放音频 / 系统设置

## ✅ AI 可执行

```bash
cmake --build cmake-build-debug --target audioctl
ctest --output-on-failure
find src include tests -name "*.[chm]" | xargs clang-format -i
```

## 👤 人类职责

1. 执行 `./scripts/install.sh install`
2. 执行 AI 提供的测试命令
3. 复制输出给 AI
4. 按 AI 要求提供日志

## 🔄 测试流程

```
AI: 编译检查 → "请安装: ./scripts/install.sh install"
人类: [安装, 复制输出]
AI: "请验证: ./audioctl virtual-status, 预期: 已绑定到XX"
人类: [执行, 反馈]
AI: [分析 → 下一步]
```

## 📋 当前测试任务

- [ ] 绑定信息持久化: use-virtual/use-physical 状态变化
- [ ] 音量同步: 物理设备音量反映到虚拟设备
- [ ] 重启保持: audioctl 重启后绑定信息仍在

## ✅ 通过标准

1. `use-virtual` 后绑定文件存在
2. `virtual-status` 显示绑定设备名+音量
3. `use-physical` 后显示"未绑定"
4. 重启后绑定信息不丢失

## 🎯 记忆点

- AI **不能**执行 install.sh
- AI **不能**假设物理状态
- 必须等人类反馈才能继续
